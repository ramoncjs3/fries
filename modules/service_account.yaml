# Service Account（机器身份）管理。
#
# **手写模块**：一次性密钥、密钥轮换、「停用即刻断开对接」这些都不是 CRUD 模板
# 能产出的。第 ⑤ 步的生成器看到 generated: false 会跳过它。
key: service_account
name: 机器账号
generated: false
scoped: false
menu: { path: /service-accounts, icon: key-round }
actions: [list, create, update, delete, rotate_key]
